[build]
  base    = "frontend"
  command = "npm run build"
  # Plugin creates output inside base (frontend); publish must point there
  publish = "frontend/.netlify/output"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--include=dev"

[[plugins]]
  package = "@netlify/plugin-nextjs"
