[build]
  base    = "frontend"
  command = "npm run build"
  # Do not set publish — @netlify/plugin-nextjs sets and creates it

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--include=dev"

[[plugins]]
  package = "@netlify/plugin-nextjs"
