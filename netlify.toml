[build]
  base    = "frontend"
  command = "npm run build"
  # Must differ from base; plugin writes to .netlify/output (repo root)
  publish = ".netlify/output"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--include=dev"

[[plugins]]
  package = "@netlify/plugin-nextjs"
