fastapi
uvicorn
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # Async SQLite driver

duckdb
pandas
numpy
pydantic>=2.7,<3
requests
beautifulsoup4
lxml

# defeatbeta-api extra Python deps that aren't in its own metadata
# NOTE: defeatbeta-api==0.0.27 must be installed with --no-deps to avoid conflicts
# Install manually: pip install --no-deps "defeatbeta-api==0.0.27"
psutil
ipython
tabulate
pyfiglet

# RRG hybrid prediction dependencies
scipy>=1.10.0  # For KD-tree spatial indexing in historical analog matching
scikit-learn>=1.3.0

# Environment variable loading
python-dotenv>=1.0.0
nltk
rich
openai